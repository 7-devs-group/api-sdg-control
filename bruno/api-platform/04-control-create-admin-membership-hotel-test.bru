meta {
  name: Criar vínculo admin no Hotel Teste
  type: http
  seq: 4
}

post {
  url: {{controlUrl}}/api/control/hotels/{{hotelTestId}}/memberships
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Administrador de Teste",
    "email": "{{adminEmail}}",
    "password": "{{adminPassword}}",
    "role": "HOTEL_ADMIN"
  }
}
