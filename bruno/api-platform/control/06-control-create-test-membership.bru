meta {
  name: Criar utilizador de teste no Hotel Teste
  type: http
  seq: 4
}

post {
  url: {{controlUrl}}/api/control/hotels/{{hotelTestId}}/memberships
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Utilizador de Teste",
    "email": "{{testEmail}}",
    "password": "{{testPassword}}",
    "role": "HOUSEKEEPER"
  }
}
